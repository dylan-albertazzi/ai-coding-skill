# Architecture Principles

Stub. Fill in with this project's real architectural principles before relying
on /tackle's "read principles.md before planning" step.

Suggested sections:

- **System overview** — major components/services and how they talk to each other
- **Boundaries** — what each package/app owns, what it must not reach into directly
- **Data flow** — how a request or job moves through the system end to end
- **Conventions** — patterns this codebase commits to (e.g. provider interfaces,
  proxy layers, error handling) so agents don't reinvent them per-ticket
- **Non-goals** — things intentionally left out of scope

Keep this short and current. It's read on every /tackle plan, so treat bloat
here as a tax on every future ticket.
